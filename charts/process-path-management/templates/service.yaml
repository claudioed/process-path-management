apiVersion: v1
kind: Service
metadata:
  name: {{ include "process-path-management.fullname" . }}
  labels:
    {{- include "process-path-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "process-path-management.selectorLabels" . | nindent 4 }}
